-- Main Script (Locked Up) - Redz Library
local redzlib = loadstring(game:HttpGet("https://raw.githubusercontent.com/minhdepzai-v/LibraryRobloc/refs/heads/main/RedzLibrary.lua"))()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")

local player = Players.LocalPlayer
while not player do task.wait() player = Players.LocalPlayer end
local playerGui = player:WaitForChild("PlayerGui")
local camera = workspace.CurrentCamera

FOV_RADIUS = 100
SMOOTHNESS = 0.80
AIM_ANGLE_OFFSET = 10.5
AIMBOT_ENABLED = true
GRAY_SKYBOX_ENABLED = false
HIDE_TAG_ENABLED = false
WANTED_ONLY = false
TARGET_UI_ENABLED = true
IGNORE_FRIENDS = true
ESP_ENABLED = false
ESP_NAMES_ENABLED = true  
local MOUSE_LOCKED = false
local target = nil
TARGET_ON_PLAYER = nil

-- ============ REDZ WINDOW ============
local Window = redzlib:MakeWindow({
  Title = "Hax do 2knw| Locked Up Panel",
  SubTitle = "by 2knw a.k.a 2kayen",
  SaveFolder = "LockedUp_Hub"
})

Window:AddMinimizeButton({
    Button = { Image = "rbxassetid://121222457209872", BackgroundTransparency = 1 },
    Corner = { CornerRadius = UDim.new(35, 1) },
})

-- ============ FOV CIRCLE ============
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "FOVGui"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

local fovCircle = Instance.new("Frame", screenGui)
fovCircle.Size = UDim2.new(0, FOV_RADIUS*2, 0, FOV_RADIUS*2)
fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
fovCircle.Position = UDim2.new(0.5, 0, 0.5, 0)
fovCircle.BackgroundColor3 = Color3.fromRGB(0,255,0)
fovCircle.BackgroundTransparency = 1
fovCircle.BorderSizePixel = 0
Instance.new("UICorner", fovCircle).CornerRadius = UDim.new(1,0)
Instance.new("UIStroke", fovCircle).Color = Color3.fromRGB(0, 255, 0)
fovCircle.UIStroke.Thickness = 1
fovCircle.Visible = AIMBOT_ENABLED

-- ============ MAIN FUNCTIONS ============
local function isVisible(targetCharacter)
	if not targetCharacter then return false end
	local hrp = targetCharacter:FindFirstChild("HumanoidRootPart")
	if not hrp then return false end
	local origin = camera.CFrame.Position
	local direction = hrp.Position - origin
	local params = RaycastParams.new()
	params.FilterType = Enum.RaycastFilterType.Blacklist
	params.FilterDescendantsInstances = {player.Character, targetCharacter}
	return Workspace:Raycast(origin, direction, params) == nil
end

local function getClosestInFOV()
	if TARGET_ON_PLAYER then
		local targetPlr = Players:FindFirstChild(TARGET_ON_PLAYER)
		if targetPlr and targetPlr.Character then
			local humanoid = targetPlr.Character:FindFirstChildOfClass("Humanoid")
			local hrp = targetPlr.Character:FindFirstChild("HumanoidRootPart")
			if humanoid and humanoid.Health > 0 and hrp then
				if WANTED_ONLY and targetPlr.Team and targetPlr.Team.Name == "Inmates" then
					local head = targetPlr.Character:FindFirstChild("Head")
					if head then
						local bb = head:FindFirstChild("OverheadBBGUI")
						if bb and bb:IsA("BillboardGui") then
							local wanted = bb:FindFirstChild("WantedTag")
							if wanted and wanted:IsA("TextLabel") then
								if not wanted.Visible then return nil end
							else return nil end
						else return nil end
					end
				end
				local screenPos, onScreen = camera:WorldToViewportPoint(hrp.Position)
				if onScreen and isVisible(targetPlr.Character) then return targetPlr end
			end
		end
		return nil
	end

	local closest = nil
	local shortestDistance = math.huge
	local screenCenter = Vector2.new(camera.ViewportSize.X/2, camera.ViewportSize.Y/2)
	local myTeam = player.Team
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= player and plr.Character then
			if IGNORE_FRIENDS and player:IsFriendsWith(plr.UserId) then continue end
			if myTeam and plr.Team == myTeam then continue end
			
			if myTeam then
				local myTeamName = myTeam.Name
				local plrTeamName = plr.Team and plr.Team.Name
				if (myTeamName == "Guards" and plrTeamName == "Warden") or (myTeamName == "Warden" and plrTeamName == "Guards") then
					continue
				end
			end
			
			local humanoid = plr.Character:FindFirstChildOfClass("Humanoid")
			local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
			if humanoid and humanoid.Health > 0 and hrp then
				if WANTED_ONLY and plr.Team and plr.Team.Name == "Inmates" then
					local head = plr.Character:FindFirstChild("Head")
					if head then
						local bb = head:FindFirstChild("OverheadBBGUI")
						if bb and bb:IsA("BillboardGui") then
							local wanted = bb:FindFirstChild("WantedTag")
							if wanted and wanted:IsA("TextLabel") then
								if not wanted.Visible then continue end
							end
						end
					end
				end
				local screenPos, onScreen = camera:WorldToViewportPoint(hrp.Position)
				if onScreen then
					local dist = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
					if dist < FOV_RADIUS and dist < shortestDistance then
						if isVisible(plr.Character) then
							closest = plr
							shortestDistance = dist
						end
					end
				end
			end
		end
	end
	return closest
end

RunService.RenderStepped:Connect(function()
	if not AIMBOT_ENABLED then
		if MOUSE_LOCKED then
			UserInputService.MouseBehavior = Enum.MouseBehavior.Default
			MOUSE_LOCKED = false
		end
		return
	end
	target = getClosestInFOV()
	if target and target.Character then
		local hrp = target.Character:FindFirstChild("HumanoidRootPart")
		if hrp then
			local camPos = camera.CFrame.Position
			local lookAtHRP = CFrame.new(camPos, hrp.Position)
			local adjustedCFrame = lookAtHRP * CFrame.Angles(math.rad(-AIM_ANGLE_OFFSET), 0, 0)
			camera.CFrame = camera.CFrame:Lerp(adjustedCFrame, SMOOTHNESS)
			if not MOUSE_LOCKED then
				UserInputService.MouseBehavior = Enum.MouseBehavior.LockCenter
				MOUSE_LOCKED = true
			end
		end
	end
end)

-- ============ SHARED FUNCTIONS ============
function enableGraySkybox()
	local lighting = game:GetService("Lighting")
	pcall(function() if lighting:FindFirstChild("GraySky") then lighting.GraySky:Destroy() end end)
	local sky = Instance.new("Sky", lighting)
	sky.Name = "GraySky"
	sky.SkyboxBk = "rbxassetid://154987705"
	sky.SkyboxDn = "rbxassetid://154987705"
	sky.SkyboxFt = "rbxassetid://154987705"
	sky.SkyboxLf = "rbxassetid://154987705"
	sky.SkyboxRt = "rbxassetid://154987705"
	sky.SkyboxUp = "rbxassetid://154987705"
end

function disableGraySkybox()
	local lighting = game:GetService("Lighting")
	pcall(function() if lighting:FindFirstChild("GraySky") then lighting.GraySky:Destroy() end end)
	pcall(function() lighting.Sky = nil end)
end

function updateTagVisibility()
	local char = player.Character
	if not char then return end
	local head = char:FindFirstChild("Head")
	if not head then return end
	for _, name in ipairs({"Hostile", "KillstreakHeadGui", "OverheadBBGUI"}) do
		local billboard = head:FindFirstChild(name)
		if billboard and billboard:IsA("BillboardGui") then
			billboard.Enabled = not HIDE_TAG_ENABLED
		end
	end
end

-- ============ ESP ============
local function applyESP(plr)
	if plr == player then return end
	local char = plr.Character
	if not char then return end
	
	local humanoid = char:FindFirstChildOfClass("Humanoid")
	if not humanoid or humanoid.Health <= 0 then return end
	
	local teamColor = Color3.fromRGB(255, 0, 0)
	if plr.TeamColor then teamColor = plr.TeamColor.Color
	elseif plr.Team and plr.Team.TeamColor then teamColor = plr.Team.TeamColor.Color end
	
	local highlight = char:FindFirstChild("ESPHighlight")
	if not highlight then
		highlight = Instance.new("Highlight", char)
		highlight.Name = "ESPHighlight"
	end
	highlight.FillColor = teamColor
	highlight.FillTransparency = 0.85
	highlight.OutlineColor = teamColor
	highlight.OutlineTransparency = 0
	
local head = char:FindFirstChild("Head")
if head then
	local billboard = head:FindFirstChild("ESPNameTag")
	if not ESP_NAMES_ENABLED then
		if billboard then billboard:Destroy() end
	elseif not billboard then
		billboard = Instance.new("BillboardGui", head)
		billboard.Name = "ESPNameTag"
		billboard.Size = UDim2.new(0, 200, 0, 30)
		billboard.StudsOffset = Vector3.new(0, 1.5, 0)
		billboard.AlwaysOnTop = true
		local label = Instance.new("TextLabel", billboard)
		label.BackgroundTransparency = 1
		label.Size = UDim2.new(1, 0, 1, 0)
		label.Text = plr.DisplayName .. " (@" .. plr.Name .. ")"
		label.TextColor3 = Color3.fromRGB(255, 255, 255)
		label.Font = Enum.Font.GothamBold
		label.TextSize = 12
		label.TextStrokeTransparency = 0
		label.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
	end
end

local function removeESP(plr)
	if plr.Character then
		local highlight = plr.Character:FindFirstChild("ESPHighlight")
		if highlight then highlight:Destroy() end
		local head = plr.Character:FindFirstChild("Head")
		if head then
			local billboard = head:FindFirstChild("ESPNameTag")
			if billboard then billboard:Destroy() end
		end
	end
end

_G.updateESP = function()
	if not ESP_ENABLED then
		for _, plr in ipairs(Players:GetPlayers()) do
			if plr ~= player then removeESP(plr) end
		end
		return
	end
	
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= player then applyESP(plr) end
	end
end

_G.refreshESP = function() _G.updateESP() end

Players.PlayerAdded:Connect(function(plr)
	plr.CharacterAdded:Connect(function()
		if ESP_ENABLED then
			task.wait(0.5)
			applyESP(plr)
		end
	end)
end)

Players.PlayerRemoving:Connect(function(plr)
	removeESP(plr)
end)

for _, plr in ipairs(Players:GetPlayers()) do
	if plr ~= player then
		plr.CharacterAdded:Connect(function()
			if ESP_ENABLED then
				task.wait(0.5)
				applyESP(plr)
			end
		end)
	end
end

-- ============ REDZ TABS ============
local AimbotTab = Window:MakeTab({"Aimbot", "crosshair"})
local VisualsTab = Window:MakeTab({"Visuals", "eye"})
local TeleportsTab = Window:MakeTab({"Teleports", "map-pin"})
local MainTab = Window:MakeTab({"Main", "settings"})

Window:SelectTab(AimbotTab)

-- ============ AIMBOT TAB ============
local AimbotSection = AimbotTab:AddSection({"Aimbot Functions"})

local ToggleAimbot = AimbotTab:AddToggle({
  Name = "Aimbot",
  Default = true,
  Callback = function(v)
    AIMBOT_ENABLED = v
    if fovCircle then fovCircle.Visible = v end
    if not v then UserInputService.MouseBehavior = Enum.MouseBehavior.Default end
  end
})

AimbotTab:AddToggle({
  Name = "Wanted Only",
  Default = false,
  Callback = function(v) WANTED_ONLY = v end
})

AimbotTab:AddToggle({
  Name = "Ignore Friends",
  Default = true,
  Callback = function(v) IGNORE_FRIENDS = v end
})

AimbotTab:AddTextBox({
  Name = "Target On",
  Description = "Type part of player name and press Enter",
  PlaceholderText = "Player name...",
  Callback = function(Value)
    if Value == "" then
      TARGET_ON_PLAYER = nil
    else
      local partial = Value:lower()
      for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player and plr.Name:lower():find(partial, 1, true) then
          TARGET_ON_PLAYER = plr.Name
          return
        end
      end
      TARGET_ON_PLAYER = nil
    end
  end
})

AimbotTab:AddSlider({
  Name = "Smooth",
  Min = 1,
  Max = 100,
  Increase = 1,
  Default = 80,
  Callback = function(Value) SMOOTHNESS = Value / 100 end
})

AimbotTab:AddSlider({
  Name = "Angle Offset",
  Min = 0,
  Max = 15,
  Increase = 1,
  Default = 10,
  Callback = function(Value) AIM_ANGLE_OFFSET = Value end
})

AimbotTab:AddSlider({
  Name = "FOV Size",
  Min = 30,
  Max = 500,
  Increase = 10,
  Default = 100,
  Callback = function(Value)
    FOV_RADIUS = Value
    if fovCircle then fovCircle.Size = UDim2.new(0, Value*2, 0, Value*2) end
  end
})

-- ============ VISUALS TAB ============
local VisualsSection = VisualsTab:AddSection({"Visual Options"})

VisualsTab:AddToggle({
  Name = "Gray Skybox",
  Default = false,
  Callback = function(v)
    if v then enableGraySkybox() else disableGraySkybox() end
  end
})

VisualsTab:AddToggle({
  Name = "ESP",
  Default = false,
  Callback = function(v)
    ESP_ENABLED = v
    _G.refreshESP()
  end
})

VisualsTab:AddToggle({
  Name = "Hide ESP Names",
  Description = "Hides the names above players when ESP is active",
  Default = false,
  Callback = function(v)
    ESP_NAMES_ENABLED = not v  
    _G.refreshESP()
  end
})

VisualsTab:AddToggle({
  Name = "Hide Tag",
  Default = false,
  Callback = function(v)
    HIDE_TAG_ENABLED = v
    updateTagVisibility()
  end
})

VisualsTab:AddToggle({
  Name = "Target UI",
  Default = true,
  Callback = function(v) TARGET_UI_ENABLED = v end
})

-- ============ TELEPORTS TAB ============
local TeleportsSection = TeleportsTab:AddSection({"Prison Teleports"})

local teleports = {
  {"Cells 1", Vector3.new(2035, 156, 2734)},
  {"Cells 2", Vector3.new(1942, 192, 2713)},
  {"Armory 1 (Prison)", Vector3.new(1841, 165, 2674)},
  {"Armory 2 (Prison)", Vector3.new(2236, 165, 2719)},
  {"Prison Tower 1", Vector3.new(1531, 186, 2731)},
  {"Prison Tower 2", Vector3.new(1849, 186, 3006)},
  {"Cafeteria", Vector3.new(1958, 156, 2684)},
  {"Front Entrance", Vector3.new(1682, 156, 2693)},
  {"Back Entrance", Vector3.new(2363, 155, 2718)},
  {"Lootboxes Area", Vector3.new(2325, 156, 2908)},
  {"Roof", Vector3.new(1794, 180, 2789)},
  {"Main Yard", Vector3.new(1848, 154, 2897)},
  {"Back Yard", Vector3.new(2051, 154, 2844)},
  {"Armory (City)", Vector3.new(1449, 67, 1530)},
  {"Criminal Base 1", Vector3.new(77, 168, 2455)},
  {"Criminal Base 2", Vector3.new(57, 135, 870)},
  {"Criminal Base 3", Vector3.new(1589, 76, 1369)},
  {"Escape the Prison", Vector3.new(1462, 172, 2720)},
}

for _, tp in ipairs(teleports) do
  TeleportsTab:AddButton({tp[1], function()
    local c = player.Character
    if c and c:FindFirstChild("HumanoidRootPart") then
      c.HumanoidRootPart.CFrame = CFrame.new(tp[2])
    end
  end})
end

-- ============ MAIN TAB ============
local MainSection = MainTab:AddSection({"Main Functions"})

-- ============ PLAYER EVENTS ============
UserInputService.InputBegan:Connect(function(input, processed)
  if processed then return end
  if input.KeyCode == Enum.KeyCode.E then
    AIMBOT_ENABLED = not AIMBOT_ENABLED
    if fovCircle then fovCircle.Visible = AIMBOT_ENABLED end
  end
end)

player.CharacterAdded:Connect(function()
  camera.CameraType = Enum.CameraType.Custom
  UserInputService.MouseBehavior = Enum.MouseBehavior.Default
  MOUSE_LOCKED = false
  if HIDE_TAG_ENABLED then
    task.wait(0.5)
    updateTagVisibility()
    task.wait(1)
    updateTagVisibility()
  end
  if ESP_ENABLED then
    task.wait(0.5)
    _G.updateESP()
  end
end)

if player.Character and HIDE_TAG_ENABLED then
  task.wait(0.5)
  updateTagVisibility()
end
